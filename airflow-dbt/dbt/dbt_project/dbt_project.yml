# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'dbt_project'
version: '1.0.0'

# This setting configures which "profile" dbt uses for this project.
profile: 'dbt_project'

# These configurations specify where dbt should look for different types of files.
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models
#
# No `staging:` node here on purpose -- confirmed with the user this
# project has no staging/bronze dbt models; bronze is populated by an
# ingestion pipeline outside dbt and only ever read via source().
#
# NOTE: this key ("dbt_project") must match `name:` above exactly, or
# dbt silently ignores this entire block and every model falls back to
# default config (view materialization, default schema) with no error.
models:
  dbt_project:
    silver:
      +schema: silver
      +materialized: table   # start simple; revisit only if run time on the
                              # full window-function rebuild becomes a real
                              # problem -- see the table-vs-incremental
                              # trade-off discussed with the user.
      archetype_a:
        +tags: ['silver', 'static_dim']
      archetype_b_real_cdc:
        +tags: ['silver', 'cdc', 'real_cdc']
      archetype_b_quasi_cdc:
        +tags: ['silver', 'cdc', 'quasi_cdc']
      archetype_c:
        +tags: ['silver', 'snapshot_dim']
      archetype_d:
        +tags: ['silver', 'parsed']
      archetype_e:
        +tags: ['silver', 'batch1_only_fact']

    gold:
      +schema: gold
      # Placeholder only -- no gold models built yet. Materialization
      # strategy for gold not decided; add it when that layer starts.